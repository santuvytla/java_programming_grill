public class firstandlastx {
    /*
    find the first and last occurance of x in sorted array.
     */
    public static void main(String[] args) {
        //we use binary search to find the first occurance of x that is greater than or equal to x, and then find we find the first occurance of greater than or equal
        // x+1 so substr 1 gives last occurance..so that will be the solution.
        //we will solve it easily later..make function for binary search taking (arr,x) for first,(arr,x-1) for second then if(first<=last) print first anf last...
        //In the function keep first as n(size of array) initially then return first as calculated.

    }
}
